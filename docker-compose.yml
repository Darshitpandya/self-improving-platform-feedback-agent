services:
  pgvector:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: feedback_agent
      POSTGRES_USER: agent
      POSTGRES_PASSWORD: agent_local_only
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
